package com.fongmi.android.tv.setting;

/** Availability of user-visible TMDB data embedded in a source detail response. */
public enum TmdbSourceState {
    ABSENT_OR_INVALID,
    IDENTITY_ONLY,
    RENDERABLE
}
