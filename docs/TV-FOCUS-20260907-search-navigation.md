# TV-FOCUS-20260907：电视搜索结果焦点回归

## 目标与范围

- 用户反馈：搜索结果顶部选项无法向右移动，另有未指明页面的按钮难以获取焦点，怀疑最近两天尤其今天的改动。
- quick-fix：修复可证实的导航回归，保留横排/竖排、筛选和结果区原有行为；继续验证可复现的其他按钮问题，不宣称一次局部修复覆盖所有未定位场景。
- 生产代码范围：`app/src/leanback/java/com/fongmi/android/tv/ui/activity/CollectActivity.java`。
- 基线：`dev2` / `1882ec67aa3eb07c4ff0e5d2abe8a4548f494e8e`。
- 保护初始 5 个 `.bak20260906*` 未跟踪备份（4 个 `MpvPlayer.java`，1 个 mobile `VideoActivity.java`）；不修改或提交。
- 开始：2026-09-07 22:35，Asia/Shanghai；初始目标 22:55 完成。设备测试准备的触摸模式、Activity 同步启动等待，以及缺失语音服务的测试前置条件导致延后；23:10 全部针对性验证结束，没有重复构建已通过的产品 APK。

## 原因与最小修复

- `CollectAdapter.onBindViewHolder()` 为横排/竖排站点共用 `onCollectKey()` 回调。
- `54b0b6a875d9d26fcf26c96ae04d4a2302e49625` 引入站点右键进入第一条搜索结果。
- 本地合并 `188553addf6692220a2a715790fb8706b2f423b0`（2026-09-07 10:54:26 +08:00）把右键拦截放到了 `isSearchLandscape()` 分流之前。
- 因而顶部横排有结果时，右键不会交给 `HorizontalGridView` 切换下一站点，而会被消费并异步转到结果区。
- 把右键进入结果的逻辑移到横排分支之后；保留竖排右键回到第一条结果、横排下键进入结果、上键到顶部按钮、首项左边界，以及 key-up 不拦截。
- 不添加全局按键拦截、静态焦点链或改动播放器依赖。属于已有导航设计的局部纠错，不需要新特性/上游设计研究。

## 相关排查

- 搜索页顶部筛选按钮已各自声明 `focusable` 并绑定左右/下键回调。
- 公共 `BaseActivity`、`KeyUtil`、`TouchOptimizationHelper` 未发现今天引入的全局方向键屏蔽；触摸优化助手最近实现日期为 2026-09-04。
- 今天 20:35 的沉浸融合 MPV 自定义按钮焦点改动仅作用于详情播放器控制条，不能解释搜索站点横排的错误分流。未把未验证的疑点当作修复依据。
- 已请求用户提供“其他按钮”的具体页面/名称；设置页和搜索输入页补测未复现同样问题，在此之前不能证明所有未指明问题已关闭。

## 验证记录

- RED：从修改前生产文件抽取完整 `onCollectKey()`，用 `javac/java` 执行行为矩阵。横排、有结果、第 0 项、右键按下触发断言失败：`unexpected result-entry interception`，准确复现错误路径。
- GREEN：从修复后生产文件抽取同一方法执行成功。矩阵为横排/竖排 × 有/无结果 × 首项/中间/末项 × 上下左右/确认 × 按下/抬起，共 120 例、480 项行为断言；验证事件消费及三个焦点请求的调用次数，而非仅检查源码字符串。首次输出的用例总数文案误写为 240，实际循环和断言数确认后已更正文案，未重复运行已经通过的行为检查。
- 临时验证材料：`/tmp/webhtv-tv-focus-20260907/`；合并差异：`/tmp/webhtv-tv-focus-20260907-merge.diff`。
- 构建：`bash ./gradlew :app:assembleLeanbackArm64_v8aDebug --console=plain` 一次通过（19 秒，129 个 task 中 114 个使用缓存）；完整输出保存在 `build.log`。未做 native/dependency 改动，没有运行 ABI 矩阵。
- APK：`app/build/outputs/apk/leanbackArm64_v8a/debug/app-leanback-arm64_v8a-debug.apk`，已安装到用户明确授权的 `192.168.50.3:5557`。未操作另外两台设备。
- 设备测试使用临时、独立的 Android instrumentation APK，反射调用当前安装 APK 的真实 Activity/Adapter，注入本地站点与结果数据，不依赖外部搜索结果；发送真实 DPAD 按下/抬起事件并断言实际焦点。测试包和材料位于上述临时目录的 `device/`，不改变产品构建配置。
- 搜索结果实际 UI：48 项断言通过（`device/results-3.log`）。覆盖有结果/空结果时横排连续右移与左移、首尾边界、站点上键到顶部、分组/相似度/显示方式双向移动、顶部下键、横排下键进入结果和结果上键返回、竖排右键进入第 0 条结果、滚动后重新从站点右键进入第 0 条、竖排上下切站点。
- 其他页面：设置页可见入口的焦点请求，以及接口行右侧按钮的左右导航、点播/直播/壁纸行的向下导航通过（`device/results-smoke.log` 内第一阶段 PASS）。
- 搜索输入页：输入框/搜索范围双向切换、向下进入屏幕键盘、键盘内向右换键，6 项断言通过（`device/results-search-input.log`）。
- 测试准备问题已分类，而非弱化产品门禁：最初直接请求焦点时仍在触摸模式，改为先发送实际方向键；同步启动等待改用 ActivityMonitor；模拟器缺少语音识别服务时 `CustomMic.initSpeech()` 正常隐藏麦克风，补测只要求当前可见按钮可聚焦。没有为通过测试修改这些产品行为。
- 每次设备测试都保存并恢复 `search_ui`、`search_column`、`keyword` 的原始值和键存在状态；测试日志确认恢复完成，不清除用户数据。已通过的搜索结果和设置页用例没有因补测搜索输入页而重跑。

## 回滚

- 使用 task guard 原子提交并创建 `recovery/TV-FOCUS-20260907/<timestamp>` annotated recovery tag；只覆盖本任务两条路径，不推送。具体提交与 tag 以 Git 中的本任务记录为准。
- 回退本次独立修复提交即可恢复基线导航；不需要回退整次合并或原有“竖排右键进入首条结果”能力。

## Recovery anchor

- 状态：搜索结果最小修复与针对性验证完成，本记录随代码由同一次 task guard finish 提交并创建本地恢复 tag。
- 完成证据：精确调用链、完整引入提交、修改前失败与修改后通过的 480 项行为断言、一次成功的 Leanback debug 构建、5557 上的搜索结果 48 项断言，以及设置页和搜索输入页补测。
- 未验证工作区代码：无；生产改动仅为 `CollectActivity.onCollectKey()` 中右键分流顺序。
- 风险边界：尚不能声称所有未指明页面的按钮问题都已修复；已补测页面未发现共同焦点失效。
- 下一步：请用户用 5557 上已安装的修复包复测，并提供仍有问题的其他页面/按钮名称以继续定位。
